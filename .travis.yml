dist: trusty
sudo: false
group: beta
language: php
php:
  - 7.0
  - hhvm
before_script:
  - composer install
  - if [[ $TRAVIS_PHP_VERSION = hhv* ]]; then echo hhvm.php7.all=1 >> /etc/hhvm/php.ini; fi
script: composer test
